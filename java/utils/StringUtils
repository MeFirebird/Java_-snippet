

前言：是啊，工具方法搞成静态的就可以了！　　

public class  final StringUtils {


   /**
     *  空字符串  代码里不要出现硬编码
     */
    public static final String EMPTY = "";
    
    
   /**
     * 判断字符串是否为空
     * @param str
     * @return
     */
    public static boolean isNullOrEmpty(String str) {
        return str == null || str.length() == 0;
    }


 /**
     * 判断多个字符串是否为空
     * @param str
     * @return
     */
    public static boolean isNullOrEmpty(String ...str){
        for(String s : str){
            if(s == null || s.length() == 0){
                return true;
            }
        }
        return false;
    }


}





















































